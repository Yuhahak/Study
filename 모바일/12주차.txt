12주차 

1. 데이터베이스 조작(insert, select 등)
- 데이터 조회 = select * from userTable;
- insert into userTable values ( ' ' , ' ');
- select id , userName from userTable;

2. 에뮬레이터에서 databases 폴더가 있는지 확인하는 함수는 ?
(getDatabasesPath)

3. 데이터베이스에서 데이터 조회 함수는?
db.rawQuery

4. Cursor란 무엇이고 자주 사용하는 함수는 ?
- 커서는 쿼리한 내용을 모두 담고 있다.
- 커서가 위치한 곳에서 데이터를 얻는 커서 객체 함수

5. 각 클래스(SQLiteOpenHelper 클래스, SQLiteDatabase 클래스, Cursor 인터페이스)
에서 주로 사용되는 메소드
